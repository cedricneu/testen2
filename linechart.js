// Werte für das Fenster festlegen 
var margin = {top: 30, right: 50, bottom: 30, left: 50 }    // Randabstände
    , width = window = window.innerWidth - margin.left-margin.right     // rechnet die Breite des Fenster
    , height = window = window.innerWidth - margin.top - margin.bottom; // rechnet die Höhe des Fenster 



d3.json("data/data.json").then((data) => {


   



})